name: cocoon
metadata:
  template: cocoon-template

services:
  api:
    project: ./backend
    language: python
    host: containerapp
    docker:
      path: ./Dockerfile
      context: ./backend

  neo4j:
    project: ./
    host: containerapp
    docker:
      path: ./Dockerfile.neo4j
      context: ./


  # Might need hooks here for better pipeline implementation?
